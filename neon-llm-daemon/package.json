{
  "name": "@blmarket/llm-daemon",
  "version": "0.7.0-alpha0",
  "description": "Launch LLM daemon",
  "main": "index.node",
  "scripts": {
    "test": "cargo test",
    "cargo-build": "cargo build --message-format=json-render-diagnostics > cargo.log",
    "cross-build": "cross build --message-format=json-render-diagnostics > cross.log",
    "postcargo-build": "neon dist < cargo.log",
    "postcross-build": "neon dist -m /target < cross.log",
    "debug": "npm run cargo-build --",
    "build": "npm run cargo-build -- --release",
    "cross": "npm run cross-build -- --release"
  },
  "author": "",
  "license": "GPL-3.0-only",
  "type": "module",
  "devDependencies": {
    "@neon-rs/cli": "0.1.82"
  }
}
